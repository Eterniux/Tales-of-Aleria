#include "GameController.h" // Include the GameController header file


int main() {
    GameController gameController; // Create an instance of GameController
    gameController.run(); // Call the run method of the GameController instance
    return 0; // Return 0 to indicate successful execution
}
